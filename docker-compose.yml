services:
  learntool:
    build: .
    container_name: learntool
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
