version: '3.8'
services:
  paperlens:
    build: .
    container_name: paperlens
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
